# Base URL the frontend uses to reach the backend API.
# Vite bakes this in at build time (import.meta.env), so changing it requires
# a rebuild (`npm run dev` restart is enough locally; a Docker image needs a
# rebuild — see the root README's Docker section).
VITE_API_URL=http://localhost:3001/api
